#!/usr/bin/python3.6
# GENERATED BY DEBIAN

import sys

# Run the main entry point similarly to how setuptools does, but because
# we didn't install the actual entry point from setup.py, don't use the
# pkg_resources API
if __name__ == '__main__':
    sys.exit(main())